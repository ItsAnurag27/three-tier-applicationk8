apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: itsanurag27/frontend:latest
        env:
        - name: API_URL
          value: http://backend:8080
        ports:
        - containerPort: 80
